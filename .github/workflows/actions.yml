name: Api CI/CD

permissions:
  id-token: write

on:
  push:
    paths:
      - 'api/**'
      - 'karthik.Common/**'
      - '.github/workflows/api.yaml'
    branches:
      - main
  pull_request:
    paths:
      - 'api/**'
      - '.github/workflows/api.yaml'
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup JDK
        uses: actions/setup-java@v2
        with:
          java-version: '11'
          distribution: 'adopt'

      - name: Upload Deployment Zip
        uses: actions/upload-arti   fanct@v2
        with:
          name: deployment_zip
          path: ./deploy.zip
  deploy_to_SIT:
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'pull_request'  # Only trigger on pull request event
    environment: SIT
    steps:
      - name: Download Deployment Zip
        uses: actions/download-artifact@v2
        with:
          name: deployment_zip

      - name: Download Deployment Script
        uses: actions/download-artifact@v2
        with:
          name: deployment_script
          path: ./az/

      - name: Verify Deployment Script
        run: chmod +x ./az/deploy-backend.sh

      - name: Execute Deployment Script
        run: ./az/deploy-backend.sh
